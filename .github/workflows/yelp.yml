name: YELP CODE KEVIN

on:
  workflow_dispatch:

jobs:

  detec-secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a one-line script
        run: echo Hello, world!

      - name: detect secrets
        run: |
          pip install detect-secrets
          detect-secrets scan --all-files
